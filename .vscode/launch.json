{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch Package",
			"type": "go",
			"request": "launch",
			"mode": "auto",
			"program": "${workspaceFolder}", // Start debugging from your project root
			"args": [], // Command-line arguments for your program (if any)
			"env": {}, // Environment variables for debugging
			"buildFlags": "", // Additional Go build flags
			"showLog": true // Show detailed debugging logs in the console
		}
	]
}